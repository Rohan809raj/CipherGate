import React from 'react';
import ReactDOM from 'react-dom/client';
import CipherGatePremium from './CipherGatePremium';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <CipherGatePremium />
  </React.StrictMode>,
);
